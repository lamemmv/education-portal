import { h, Component } from 'preact';
import NewsCreateContainer from './news.create.container';

class NewsCreateIndex extends Component {
  render() {
    return (
      <NewsCreateContainer />
    );
  }
}

export default NewsCreateIndex;
